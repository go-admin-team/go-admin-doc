{
  "name": "go-admin-doc",
  "version": "0.0.1",
  "description": "go-admin-doc",
  "license": "MIT",
  "packageManager": "pnpm@9.15.1",
  "scripts": {
    "build": "dumi build && node scripts/postbuild-seo.mjs",
    "dev": "dumi dev",
    "start": "npm run dev"
  },
  "commitlint": {
    "extends": [
      "@commitlint/config-conventional"
    ]
  },
  "lint-staged": {
    "*.{md,json}": [
      "prettier --write --no-error-on-unmatched-pattern"
    ]
  },
  "devDependencies": {
    "@commitlint/cli": "^19.8.0",
    "@commitlint/config-conventional": "^19.8.0",
    "dumi": "^2.4.20",
    "husky": "^9.1.7",
    "lint-staged": "^15.5.0",
    "prettier": "^3.5.3"
  },
  "authors": [
    "zwj777@live.com"
  ]
}
